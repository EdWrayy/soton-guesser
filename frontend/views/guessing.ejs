<!doctype html>
<html lang="en">
    <h1>{{timerText}}</h1>
    <!-- <div v-if="state.isAdmin && guessTime == 0">
        <button class="btn btn-lg btn-primary" @click="advance">Next</button>
    </div> -->
    <div v-if="!awnsered">
        <button class="btn btn-lg btn-primary" @click="awnser">Submit answer</button>
    </div>
    <div v-else>
        <p>Guess submitted. Waiting for the round to end.</p>
    </div>
    <div v-if="location != null">
            <h3>{{location.name}}</h3>
    </div>
    <div class="guessing">
        <img src="" id="guessImage">
        <%- include('map'); -%>
    </div>
</html>
